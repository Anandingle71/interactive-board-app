<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The drawing surface fills the screen, toolbar floats on top. -->
    <com.boardapp.annotate.ink.InkSurfaceView
        android:id="@+id/inkView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <HorizontalScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="12dp"
        android:background="@drawable/toolbar_bg"
        android:paddingHorizontal="8dp"
        android:paddingVertical="6dp"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:divider="@null"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <Button android:id="@+id/btnPen" style="@style/Tool" android:text="Pen" />
            <Button android:id="@+id/btnEraser" style="@style/Tool" android:text="Eraser" />

            <View style="@style/Sep" />

            <View android:id="@+id/colBlack" style="@style/Swatch" android:background="#000000" />
            <View android:id="@+id/colRed" style="@style/Swatch" android:background="#E53935" />
            <View android:id="@+id/colBlue" style="@style/Swatch" android:background="#1E88E5" />
            <View android:id="@+id/colGreen" style="@style/Swatch" android:background="#43A047" />

            <View style="@style/Sep" />

            <Button android:id="@+id/btnThin" style="@style/Tool" android:text="S" />
            <Button android:id="@+id/btnMedium" style="@style/Tool" android:text="M" />
            <Button android:id="@+id/btnThick" style="@style/Tool" android:text="L" />

            <View style="@style/Sep" />

            <Button android:id="@+id/btnUndo" style="@style/Tool" android:text="Undo" />
            <Button android:id="@+id/btnRedo" style="@style/Tool" android:text="Redo" />
            <Button android:id="@+id/btnClear" style="@style/Tool" android:text="Clear" />
        </LinearLayout>
    </HorizontalScrollView>
</FrameLayout>
